Write generated test source files under `{{strategy_attempt_test_dir}}` and mirror the files under `{{generated_tests_dir}}`.
`generated-tests.json` must be a JSON object with `schema_version: "1.0"`, `run_id`, `node_id`, and `generated_tests`. Use `generated_tests` as the only file list. Each `generated_tests` entry must include `path` using the `generated-tests/<file>` prefix plus `language`, `framework`, and `description` when known. Use an empty `generated_tests` array if no tests were produced.
