## Generated-test Bundle Instructions

This node is a generated-test producer. Author runnable generated test source files under {{strategy_attempt_test_dir}}, mirror every declared bundle file under {{generated_tests_dir}}, and write the manifest to exactly {{generated_tests_manifest_path}}.

Read the exact pinned schema named by `Validate against` in the central Ultrafuzz Output Contract and run the exact `Validation command` rendered beside it. That schema alone owns the manifest's JSON version, field names, types, required and optional members, and empty form.

Bind `run_id` exactly to {{run_id}} and `node_id` exactly to {{logical_node_id}}. The node identity is the logical producer shown here, not a concrete attempt, loop, or model-fanout identity. Bind the bundle framework to the one checked-in native framework used by the whole atomic bundle. Never mix frameworks in one bundle or assign a separate framework to an individual companion. These run, producer, and repository-context bindings are checked beyond JSON Schema.

Classify only independently runnable tests and reproducers as generated tests. Classify imported helper libraries, mocks, fixtures, deployment scripts, text data fixtures, and other non-runnable dependencies as support files. Every declared companion, including data fixtures, must be strict UTF-8 text rather than binary data and must live beneath the artifact bundle, never at a workspace path. When no runnable test was produced, use the empty bundle defined by the schema while still binding its framework to the checked-in native framework. Before returning, validate after every declared companion is present, singly linked, regular, non-empty, and neither a symlink nor reachable through one.
