Write runnable generated test source files under `{{strategy_attempt_test_dir}}` and mirror every declared bundle file under `{{generated_tests_dir}}`.
`generated-tests.json` must be a closed JSON object with `schema_version: "ultrafuzz.generated-tests.v3"`, current `run_id`, current logical `node_id`, `generated_tests`, and `support_files`. Put only independently runnable test/reproducer files in `generated_tests`. Put imported helper libraries, mocks, fixtures, deployment scripts, text data fixtures, and other non-runnable dependencies in `support_files`; never misclassify them as runnable tests. Every test and support companion, including data fixtures, must be strict UTF-8 text; binary files are not supported. Both arrays use the same closed entry shape: required `path` under `generated-tests/<relative-file>` and optional `size_bytes`, `sha256`, `language`, `framework`, `description`, and documented closed `provenance` fields. Paths must be unique across both arrays. A non-empty `support_files` array requires at least one `generated_tests` entry. Use both arrays empty when no runnable test was produced. Before returning, validate the final manifest with the exact validation command in the output contract after every declared companion is present, regular, non-empty, and not a symlink.
