Write generated test source files under `{{strategy_attempt_test_dir}}` and mirror the files under `{{generated_tests_dir}}`.
`generated-tests.json` must be a closed JSON object with `schema_version: "ultrafuzz.generated-tests.v2"`, `run_id`, `node_id`, and `generated_tests`. Use `generated_tests` as the only file list. Each entry includes `path` using the `generated-tests/<file>` prefix and may include `size_bytes`, `sha256`, `language`, `framework`, `description`, and the documented closed provenance fields. Use an empty `generated_tests` array if no tests were produced.
