## Ultrafuzz Output Contract

## No-Fuzz Ablation Mode

This branch is the no-fuzz ablation for the benchmark. If any earlier prompt
instruction conflicts with this section, this section wins.

- Do not create, edit, copy, materialize, compile, or execute fuzz tests,
  property-based tests, invariant tests, generated Foundry `.t.sol` tests,
  Recon/Echidna/Medusa campaigns, or coverage-guided fuzzer runs.
- Do not run `forge test`, Recon, Echidna, Medusa, coverage tooling, or
  generated-test validation. Inspect existing source, docs, configuration,
  tests, and current-run artifacts only when they help reason about bugs.
- Keep the property-specification phase and each strategy's bug-search lens.
  Search directly for bugs associated with the identified properties and write
  concrete production findings to `findings.json`.
- When `generated-tests.json` is required, write a valid empty manifest. Review
  nodes that normally aggregate generated tests must write empty aggregation
  manifests instead of copying files.

Before finishing, you MUST ensure these required artifacts exist exactly at these paths:
{{required_artifact_paths}}
{{findings_contract}}{{generated_tests_contract}}{{boundary_recipes_contract}}

After completing the work and writing every required artifact, your final response MUST contain ONLY one raw, valid JSON object in exactly this form:

{"summary":"A concise description of the completed work and written artifacts."}

The `summary` field is REQUIRED and MUST be a JSON string. Do NOT include Markdown fences, introductory text, explanations, or any other content before or after the JSON object. Do NOT omit the JSON object even when the work succeeds.
