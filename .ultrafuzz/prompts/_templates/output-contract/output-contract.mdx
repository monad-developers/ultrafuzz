## Ultrafuzz Output Contract

Before finishing, you MUST ensure every artifact below exists and satisfies its named contract. A file merely existing is not sufficient:

{{artifact_contracts}}

After completing the work and writing every required artifact, your final response MUST contain ONLY one raw, valid JSON object in exactly this form:

{"summary":"A concise description of the completed work and written artifacts."}

The `summary` field is REQUIRED and MUST be a JSON string. Do NOT include Markdown fences, introductory text, explanations, or any other content before or after the JSON object. Do NOT omit the JSON object even when the work succeeds.
