## Ultrafuzz Output Contract

Before finishing, you MUST ensure every artifact below exists and satisfies its named contract. A file merely existing is not sufficient:

{{artifact_contracts}}

{{schema_guidance}}Artifact writing is part of the task, not optional final-response metadata. Create parent directories before writing and write each artifact to the exact absolute path shown above. Also verify every path exists, is non-empty unless its contract explicitly allows an empty form, and matches its JSON or Markdown contract. Do not write required artifacts only under the repository root, a relative working directory, or a different similarly named path.

For every schema-backed JSON artifact:

1. Write the artifact in its canonical schema.
2. After your final write and before returning the node's final response, run every exact `Validation command` and `Contract validation command` shown above.
3. If a command exits 1, correct that artifact yourself and rerun the same command during this session.
4. If you change an artifact after it passes validation, rerun its command.
5. Never edit a supplied schema. Its bytes are pinned and the command accepts it only when they match the trusted digest.
6. Exit 2 is a setup or tool failure. Do not edit the schema or claim validation succeeded.
7. Finish only after every displayed validation command exits 0.
8. The validation command never modifies the artifact.
9. Contract validation covers document-local semantics only. Host semantic and context verification still runs after you finish.

After completing the work and writing every required artifact, the declared artifacts are the task result. Any terminal response is informational only: it has no required schema and does not replace writing or validating the artifacts.
