For every output declared with `Contract: ultrafuzz/boundary-recipes@1`, read the exact pinned schema named by `Validate against` in the central Ultrafuzz Output Contract and run the exact `Validation command` rendered beside that output. The declared path is authoritative; do not create a differently named structured artifact. That schema alone owns the JSON version, field names, types, required and optional members, and empty form. When a human-readable boundary-recipe companion is declared, keep it and the structured artifact semantically aligned with the same source-backed boundary recipes; this contextual correspondence is checked separately from JSON Schema validation.
