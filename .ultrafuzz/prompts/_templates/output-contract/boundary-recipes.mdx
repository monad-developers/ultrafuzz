`boundary-recipes.md` should be a markdown index of reproduction recipes. `boundary-recipes.json` must be a JSON object with `schema_version: "1.0"` and a `recipes` array. Each recipe should include `id`, `title`, `path`, and any known `finding_ids`, `property_ids`, or `summary`. Use an empty `recipes` array if no recipes were produced.
