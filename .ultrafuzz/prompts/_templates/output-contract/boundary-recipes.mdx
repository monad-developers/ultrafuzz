`boundary-recipes.md` is the human-readable index. For `boundary-recipes.json`, read the exact pinned schema named by `Validate against` in the central Ultrafuzz Output Contract and run the exact `Validation command` rendered beside it. That schema alone owns the JSON version, field names, types, required and optional members, and empty form. Keep the Markdown index and structured artifact semantically aligned with the same source-backed boundary recipes; this contextual correspondence is checked separately from JSON Schema validation.
