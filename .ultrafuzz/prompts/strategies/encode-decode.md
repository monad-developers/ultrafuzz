---
id: encode-decode
display_name: Encode / Decode
---

# Encode Decode

Read setup and properties:

{{artifact_handoff:base-test-setup}}
{{artifact_handoff:property-specification-fanin}}

Run strategy `{{strategy}}` attempt `{{attempt_index}}` loop `{{strategy_loop_index}}` of `{{strategy_loop_count}}`.
Write tests under `{{strategy_attempt_test_dir}}`, findings to `{{output_findings_path}}`, patches to `{{output_patch_path}}`, and metadata to `{{artifact_path}}/generated-tests.json`.
