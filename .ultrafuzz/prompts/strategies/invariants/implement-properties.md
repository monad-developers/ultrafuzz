---
id: stateful-invariant-implement-properties
display_name: Implement properties
---

# Implement Stateful Properties

Read invariant sources:

{{artifact_path:property-specification-fanin}}
{{artifact_path:stateful-invariant-setup}}
{{artifact_path:stateful-invariant-handlers}}
{{artifact_path:stateful-invariant-coverage}}

Use priorities `{{invariant_property_priorities}}`, filter `{{invariant_property_priority_filter}}`, and threshold `{{invariant_property_priority_threshold}}`.
Write patches to `{{output_patch_path}}`, findings to `{{output_findings_path}}`, and artifacts under `{{artifact_dir}}`.
