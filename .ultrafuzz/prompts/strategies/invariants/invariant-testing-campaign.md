---
id: stateful-invariant-recon-campaign
display_name: Invariant testing campaign
---

# Invariant Testing Campaign

Read invariant sources:

{{artifact_path:property-specification-fanin}}
{{artifact_path:stateful-invariant-coverage}}
{{artifact_path:stateful-invariant-implement-properties}}

Run fuzzing for `{{invariant_testing_fuzzer_timeout}}`, write patches to `{{output_patch_path}}`, findings to `{{output_findings_path}}`, and artifacts under `{{artifact_dir}}`.
