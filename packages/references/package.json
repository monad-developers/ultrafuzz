{
  "name": "@ultrafuzz/references",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @ultrafuzz/artifacts build && tsc -p tsconfig.json && cp ../../.ultrafuzz/references.yml dist/default-references.yml",
    "test": "pnpm --filter @ultrafuzz/references build && rm -rf dist-test && tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js",
    "typecheck": "pnpm --filter @ultrafuzz/artifacts build && tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@ultrafuzz/artifacts": "workspace:*",
    "yaml": "^2.8.0"
  }
}
