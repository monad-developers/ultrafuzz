{
  "name": "@ultrafuzz/runtime",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && cp ../../.ultrafuzz/topology.yml dist/topology.yml && rm -rf dist/templates && cp -R src/templates dist/templates && rm -rf dist/schema && cp -R schema dist/schema && node scripts/verify-schema-registry.mjs",
    "test": "pnpm --filter @ultrafuzz/runtime... build && rm -rf dist-test && tsc -p tsconfig.test.json && node scripts/run-tests.mjs && bun test dist-test/test/runtime.test.js --test-name-pattern '^Bun adapter contract:'",
    "test:pr-smoke:prebuilt": "rm -rf dist-test && tsc -p tsconfig.test.json && node scripts/run-pr-smoke-tests.mjs",
    "test:release:runtime-shard": "pnpm --filter @ultrafuzz/runtime... build && rm -rf dist-test && tsc -p tsconfig.test.json && node scripts/run-runtime-test-shard.mjs",
    "test:release:supporting": "pnpm --filter @ultrafuzz/runtime... build && rm -rf dist-test && tsc -p tsconfig.test.json && node scripts/run-tests.mjs supporting && bun test dist-test/test/runtime.test.js --test-name-pattern '^Bun adapter contract:'",
    "test:kimi-contract": "pnpm --filter @ultrafuzz/runtime... build && rm -rf dist-test && tsc -p tsconfig.test.json && bun test dist-test/test/runtime.test.js --test-name-pattern '^Bun adapter contract: generated Kimi'",
    "typecheck": "pnpm --filter @ultrafuzz/runtime^... build && tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@smthrs/driver": "0.35.0",
    "@ultrafuzz/artifacts": "workspace:*",
    "@ultrafuzz/config": "workspace:*",
    "@ultrafuzz/prompts": "workspace:*",
    "@ultrafuzz/references": "workspace:*",
    "@ultrafuzz/security": "workspace:*",
    "@ultrafuzz/topology": "workspace:*",
    "effect": "4.0.0-beta.105",
    "entities": "7.0.1",
    "frog": "1.1.0",
    "mdast-util-from-markdown": "2.0.3",
    "npm": "11.19.0",
    "proper-lockfile": "4.1.2",
    "typescript": "6.0.3",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@moonshot-ai/kimi-code": "0.29.1",
    "@types/proper-lockfile": "^4.1.4",
    "smthrs": "0.35.0"
  }
}
